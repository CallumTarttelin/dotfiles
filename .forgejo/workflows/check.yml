name: Check
on: [push, pull_request]

jobs:
  lint:
    runs-on: nix-native
    steps:
      - uses: actions/checkout@v4
      - name: Format (alejandra)
        run: nix run nixpkgs#alejandra -- --check .

  check:
    runs-on: nix-native
    steps:
      - uses: actions/checkout@v4
      - name: Flake check
        run: nix flake check

  build:
    runs-on: nix-native
    strategy:
      matrix:
        host: [nixshark, nixwork, nixie]
    steps:
      - uses: actions/checkout@v4
      - name: Build ${{ matrix.host }}
        run: nix build .#nixosConfigurations.${{ matrix.host }}.config.system.build.toplevel

  auto-merge:
    runs-on: nix-native
    needs: [lint, check, build]
    if: github.event_name == 'pull_request' && github.event.pull_request.head.ref == 'auto/flake-update'
    steps:
      - name: Merge PR
        env:
          FLAKE_UPDATER_TOKEN: ${{ secrets.FLAKE_UPDATER_TOKEN }}
        run: |
          : "${FLAKE_UPDATER_TOKEN:?Set the FLAKE_UPDATER_TOKEN Actions secret to a personal access token. Do not use FORGEJO_TOKEN; Forgejo reserves that for the automatic workflow token.}"
          curl -sSf -X POST \
            -H "Authorization: token $FLAKE_UPDATER_TOKEN" \
            -H "Content-Type: application/json" \
            "https://git.callumtarttelin.com/api/v1/repos/tarttelin/dotfiles/pulls/${{ github.event.pull_request.number }}/merge" \
            -d '{"Do":"squash","delete_branch_after_merge":true}'
